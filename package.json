{
  "name": "@fagemx/thyra",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.11",
    "better-sqlite3": "^11",
    "hono": "^4",
    "zod": "^3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7",
    "@types/bun": "^1.3.10",
    "typescript": "^5",
    "vitest": "^2"
  },
  "engines": {
    "node": ">=22"
  }
}
