# A1: WorldManager Orchestrator Class

> **Layer**: L0
> **Dependencies**: 無
> **Blocks**: A2, A3, 所有 L1/L2 Tracks
> **Output**: `src/world-manager.ts`

Same as v1 — see previous A1. Issue: #178.
Rescue worktree code, add tests, commit.

## 驗收
```bash
bun run build && bun run lint && bun test src/world-manager.test.ts
```
